{
  "name": "ishuin-nim-proxy",
  "version": "1.0.0",
  "description": "NVIDIA NIM proxy for portfolio AI features",
  "type": "module",
  "main": "api/nim.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
