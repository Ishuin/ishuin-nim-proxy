{
  "name": "ishuin-nim-proxy",
  "version": "1.0.0",
  "description": "NVIDIA NIM proxy for portfolio AI features",
  "main": "api/nim.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
